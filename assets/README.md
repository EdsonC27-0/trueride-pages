# Hero phone video

Add the autoplay video here as:

`hero-phone.mp4`

Recommended export:

- Portrait 9:16
- MP4 with H.264 video
- 720 × 1280 or 1080 × 1920
- No audio track, or audio muted
- Keep the file below roughly 8 MB for faster loading

The landing page already includes `autoplay`, `muted`, `loop`, and `playsinline`.
If the file is absent or cannot play, the static vehicle artwork remains visible.
